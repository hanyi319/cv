{
  "name": "cv",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "rm -rf dist && parcel build src/index.html --public-url ./"
  },
  "devDependencies": {
    "parcel": "latest"
  }
}
